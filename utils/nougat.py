from .common import nougat

# Get the text from Image
def nougat_text(image):
    print(dir(nougat))
    # Initialize the Nougat OCR model
    # ocr_model = nougat.OCR()

    # Use pytesseract to extract text from the PIL Image
    # text = ocr_model.extract_text(image)

    return "Hello Very Very Very"
